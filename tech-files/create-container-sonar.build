# 1. Detenemos el contenedor que está consumiendo esos 2GB de RAM
docker rm -f sonarqube_server

# 2. Eliminamos el volumen de datos corrupto
docker volume rm sonarqube_data

docker run -d --name sonarqube_server \
  -p 9000:9000 \
  -v sonarqube_data:/opt/sonarqube/data \
  -v sonarqube_extensions:/opt/sonarqube/extensions \
  -v sonarqube_logs:/opt/sonarqube/logs \
  sonarqube:10.7-community

